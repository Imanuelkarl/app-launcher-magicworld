import { Boxes, LogOut } from 'lucide-react'; import { Link } from 'react-router-dom'; import type { User } from '../types';
export function Header({ user, logout }: { user?: User | null; logout?: () => void }) { return <header className="border-b border-violet-100 bg-white/80 backdrop-blur"><div className="shell flex h-18 items-center justify-between py-4"><Link to="/" className="flex items-center gap-2 font-extrabold"><span className="grid h-8 w-8 place-items-center rounded-lg bg-ink text-white"><Boxes size={17}/></span>Magic Worlds</Link><nav className="flex items-center gap-5"><Link className="nav-link" to="/">Hub</Link>{user && <><Link className="nav-link" to="/manage">Dashboard</Link>{user.role === 'admin' && <Link className="nav-link" to="/team">Team</Link>}<button className="btn-subtle !px-3 !py-2" onClick={logout}><LogOut size={15}/><span className="hidden sm:inline">Sign out</span></button></>}</nav></div></header>; }
