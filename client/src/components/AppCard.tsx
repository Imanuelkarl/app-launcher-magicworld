import { ArrowUpRight } from 'lucide-react'; import type { LauncherApp } from '../types'; import { AppIcon } from './AppIcon';
export function AppCard({ app }: { app: LauncherApp }) { const url = app.webUrl || app.downloadUrl; return <article className="panel p-5 hover:-translate-y-1 hover:shadow-glow"><div className="flex justify-between"><AppIcon type={app.type}/><div className="flex gap-1">{app.isNew && <span className="rounded bg-emerald-50 px-2 py-1 text-[10px] font-bold text-emerald-700">NEW</span>}{app.updateAvailable && <span className="rounded bg-amber-50 px-2 py-1 text-[10px] font-bold text-amber-700">UPDATE</span>}</div></div><p className="mt-5 text-[10px] font-bold uppercase tracking-widest text-slate-400">{app.type} · {app.category}</p><h3 className="mt-1 font-bold">{app.name}</h3><p className="mt-2 min-h-10 text-sm leading-5 text-slate-500">{app.description}</p><div className="mt-5 flex items-center justify-between text-xs font-bold text-slate-400"><span>{app.platforms.join(' · ')}</span>{url && <a className="flex items-center gap-1 text-magic" href={url} target="_blank" rel="noreferrer">Open <ArrowUpRight size={14}/></a>}</div></article>; }
